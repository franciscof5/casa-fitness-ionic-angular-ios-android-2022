import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.f5sites.casafitness',
  appName: 'CasaFitness',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
